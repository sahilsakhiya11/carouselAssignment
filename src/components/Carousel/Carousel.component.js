import React from "react";
import PropTypes from "prop-types";

const Carousel = ({ url, loading }) => {};

Carousel.propTypes = {
    url: PropTypes.string,
    loading: PropTypes.bool
}

export default Carousel;
